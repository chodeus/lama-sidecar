# torch is installed separately in the Dockerfile from the CPU wheel index
# to avoid pulling ~2GB of CUDA libraries we don't use.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.13.4
pillow==11.1.0
numpy==2.2.1
