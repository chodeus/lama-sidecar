# torch is installed separately in the Dockerfile from the CPU wheel index
# to avoid pulling ~2GB of CUDA libraries we don't use.
fastapi==0.138.2
uvicorn[standard]==0.49.0
pydantic==2.13.4
pillow==12.2.0
numpy==2.5.1
# detect.py runs the PP-OCRv6 det model under onnxruntime; the model file is
# fetched lazily from the pinned rapidocr wheel (see detect.py — the rapidocr
# package itself needs full opencv-python, whose Qt links libGL the slim image
# doesn't have).
onnxruntime==1.27.0
