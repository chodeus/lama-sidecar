name: Release Please

on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write
  packages: write

jobs:
  release:
    uses: chodeus/chodeus-ops/.github/workflows/release-please.yml@6830c339d8517809f8bab5ff6e9179f60f92e891 # main

  # Tags created by release-please use GITHUB_TOKEN, which does not trigger
  # other workflows — so we publish the image here, gated on the release
  # output, instead of relying on a push:tags trigger.
  docker:
    needs: release
    if: needs.release.outputs.release_created == 'true'
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/publish-image.yml
    with:
      tag: ${{ needs.release.outputs.tag_name }}
